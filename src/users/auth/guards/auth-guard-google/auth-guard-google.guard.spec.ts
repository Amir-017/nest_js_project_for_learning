import { AuthGuardGoogleGuard } from './auth-guard-google.guard';

describe('AuthGuardGoogleGuard', () => {
  it('should be defined', () => {
    expect(new AuthGuardGoogleGuard()).toBeDefined();
  });
});
